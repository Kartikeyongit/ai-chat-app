DATABASE_URL="postgresql://postgres:YOUR_PASSWORD@localhost:5432/ai-chat-app"
GEMINI_API_KEY="your-gemini-api-key"
PORT=4000
AUTH_SECRET="your-auth-secret-change-in-production"

# Mock mode speed tuning (no GEMINI_API_KEY set):
# MOCK_DELAY=600     # First-token latency in ms (default: 600)
# MOCK_SPEED=35      # Base delay per word in ms (default: 35)
